<Page
    x:Class="HolidayMakerClient.View.MyPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HolidayMakerClient.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:holidaymakerclient="using:HolidayMakerClient"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="1">
            <ListView BorderBrush="Black"
                      BorderThickness="2"
                      ItemsSource="{x:Bind MyReservations}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="holidaymakerclient:Reservation">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{x:Bind HomeId}"/>
                            <TextBlock Text="{x:Bind TotalPrice}"/>
                        </StackPanel>
                        
                    </DataTemplate>
                </ListView.ItemTemplate>
                
            </ListView>
           
            
        </StackPanel>

    </Grid>
</Page>
